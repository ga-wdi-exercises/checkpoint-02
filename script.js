var colors = ["red", "white", "blue"];// Create an array called `colors` that contains three colors.



// Log the last item in the array to the console.
console.log("colors" blue);

// Create a `for` loop that prints out "My favorite color is [insert color here]" for each item in `colors`.
for (i=0, i<colors.length; i++) {
    console.log("My favorite color is" + colors[i]);
}


// Create an `instructor` object that contains three key-value pairs.
var instructor = {
  Name: "Andy"
  Class: "WDI"
  Time: "9:00am"
}

var instructor = {
  Name: "Andy"
  Class: "WDI"
  Time: "9:00am"
  facial-hair: "small beard"// Add a `facial-hair` property to `instructor`.

// Log the value of the `facial-hair` property to the console.
console.log(small beard)
