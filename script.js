var colors = [
    "red",
    "yellow",
    "green"
];
console.log("green")

for (var colors = 0; colors < 3; colors ++)
  console.log("My favorite color is red")
  console.log("My favorite color is yellow")
  console.log("My favorite color is green")

var instructor = function (Adrian){
  instructor.name = Adrian,
  instructor.school = GeneralAssembly,
  instructor.class = WebDevelopmentImmersive,
  instructor.facialhair = Beard
  console.log(facialhair)
};


// Create an array called `colors` that contains three colors. (x)

// Log the last item in the array to the console. (x)

// Create a `for` loop that prints out "My favorite color is [insert color here]" for each item in `colors`. (x)

// Create an `instructor` object that contains three key-value pairs.

// Add a `facial-hair` property to `instructor`.

// Log the value of the `facial-hair` property to the console.
